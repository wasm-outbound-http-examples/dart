name: browser_and_node_js_interop_unsafe_fetch
description: Make HTTP requests from inside WASM in Dart using raw "fetch()" via "js_interop_unsafe".
version: 1.0.0
repository: https://github.com/wasm-outbound-http-examples/dart

environment:
  sdk: ^3.5.2

dependencies:
  web: ^0.5.1

dev_dependencies:
  build_runner: ^2.4.8
  build_web_compilers: ^4.0.9
  lints: ^4.0.0
